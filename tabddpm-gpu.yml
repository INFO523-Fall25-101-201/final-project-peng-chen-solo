name: tabddpm_gpu
channels:
  - pytorch
  - nvidia
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - pip
  - numpy
  - pandas
  - scikit-learn
  - pytorch=2.1.2
  - pytorch-cuda=11.8
  - pip:
      - tqdm
      - pyyaml
      - torchmetrics
      - scipy
      - joblib
      - category_encoders
